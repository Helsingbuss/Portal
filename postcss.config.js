export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
// This is a PostCSS configuration file that sets up Tailwind CSS and Autoprefixer as plugins.
// It exports an object with a `plugins` property, where Tailwind CSS and Autoprefixer are initialized.
// This configuration allows for processing CSS files with Tailwind CSS utilities and automatically adding vendor prefixes for better browser compatibility.
// It does not include any custom PostCSS plugins or additional configurations. 
