/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
// This is a Tailwind CSS configuration file that specifies the content files to scan for class names and extends the default theme.
// It does not include any custom plugins or additional configurations.
// The `content` array includes HTML and JavaScript/TypeScript files in the `src` directory.
// The `theme` object allows for extending the default Tailwind CSS theme, but currently does not add any custom styles.          